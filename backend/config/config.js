const config = {
    FRONTEND_URL: process.env.FRONTEND_URL || 'https://ecommerce-fullstack-frontend-1.onrender.com',
    BACKEND_URL: process.env.BACKEND_URL || 'https://ecommerce-fullstack-tvzc.onrender.com',
    NODE_ENV: process.env.NODE_ENV || 'development',
    LOCALHOST_URL: "http://localhost:5000"
};

module.exports = config;